spring:
  data:
    mongodb:
      uri: ${MONGO_URI}
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

server:
  port: 8082

management:
  endpoints:
    web:
      exposure:
        include: "*"

#management:
#  endpoints:
#    exposure:
#      include: "*"
